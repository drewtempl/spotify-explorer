version: '3'
services:
  
  server:
    build:
      context: server
      dockerfile: Dockerfile
    image: explorer-server
    ports:
      - '5000:5000'

  client:
    build:
      context: client
      dockerfile: Dockerfile
    image: explorer-client
    ports:
      - "3000:80"

